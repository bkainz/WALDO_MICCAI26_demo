# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
Pillow>=9.0.0

# Deep learning (DINOv3-ViT-B/16 patch features for Wasserstein reference selection)
torch>=2.0.0
torchvision>=0.15.0
# DINOv3 AutoModel support requires a recent transformers (>=4.53). Older versions
# still work via the automatic DINOv2-base fallback in reference_selector.py.
transformers>=4.44.0

# VLM API clients
openai>=1.0.0

# Dataset handling
huggingface_hub>=0.19.0
datasets>=2.14.0

# Progress bars and utilities
tqdm>=4.65.0

# Optional: For CXR DICOM preprocessing
# pydicom>=2.4.0

# Optional: For visualization
# matplotlib>=3.7.0
